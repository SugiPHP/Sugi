<?php
/**
 * SugiPHP Logger settings demo
 */

return [
    // "filename"   => "/tmp/custom.log", // empty means to use default error_log() function for logging
    // "level"      => "info", // default value is "debug" if $app["debug"] is TRUE, "info" on FALSE
    // "logFormat"  => "[{datetime}] [{level}] {message} {context}",
    // "dateFormat" => "Y-m-d H:i:s",
    // "eol"        => "\n",
];
